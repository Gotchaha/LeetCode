// learn Standard Template Library
#include <cstdio>
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <string>
#include <sstream>
#include <map>
using namespace std;

// vector
// const int maxn = 30;
// int n;
// vector<int> a[maxn];

// set
// set<string> dict;

// map
// map<string,int> cnt;

int main() {
    // string s, buf;
    // while (cin >> s)
    // {
    //     for (int i = 0; i < s.length(); i++)
    //         if (isalpha(s[i])) s[i] = tolower(s[i]); else s[i] = ' ';  // 对输入的字符串处理：把非字母的字符变成空格
    //     stringstream ss(s);
    //     while(ss >> buf) dict.insert(buf);
    // }

    // //迭代器
    // for(set<string>::iterator it = dict.begin(); it != dict.end(); ++it)
    //     cout << *it << "\n";


    
    return 0;
}